/**
 * DEPRECATED — ImagePlaceholder
 * ===========================================================================
 * Replaced by components/ui/MediaFrame.tsx in the v3 redesign, which prefers a
 * real photograph whenever one is supplied and falls back to drawn artwork
 * otherwise. MediaFrame still exports an `ImagePlaceholder` alias, re-exported
 * here so a lingering import from a previous version resolves.
 *
 * Nothing in the live site imports this file. SAFE TO DELETE.
 * ===========================================================================
 */

export { ImagePlaceholder } from './MediaFrame';
