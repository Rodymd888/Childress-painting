/**
 * DEPRECATED — IndustryMarquee
 * ===========================================================================
 * Replaced by components/home/TrustedBy.tsx in the v3 redesign, which scrolls
 * real client names from the qualifications record rather than a generic list
 * of sector labels. Re-exported under the old name so a lingering import from
 * a previous version resolves and compiles.
 *
 * Nothing in the live site imports this file. SAFE TO DELETE.
 * ===========================================================================
 */

export { TrustedBy as IndustryMarquee } from './TrustedBy';
