/**
 * DEPRECATED — MarketCard
 * ===========================================================================
 * Replaced by components/cards/IndustryCard.tsx in the v3 redesign. Re-exported
 * here under the old name so a lingering import from a previous version
 * resolves and compiles instead of failing the build.
 *
 * Nothing in the live site imports this file. SAFE TO DELETE.
 * ===========================================================================
 */

export { IndustryCard as MarketCard } from './IndustryCard';
